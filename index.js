
module.exports = function console2(element) {
    if (string !== null) throw new TypeError("Frontis wants a something to log!");
    const render = element.toString() + ': ' + element;
    return console.log(render);
};
